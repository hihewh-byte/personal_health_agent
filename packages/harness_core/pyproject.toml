[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "harness-core"
version = "0.0.0a2"
description = "Thin domain-agnostic agent harness control-plane interfaces (TurnPlan, FSM, integrity, frozen DomainAdapter contract)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "hihewh-byte" }]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
loop = []  # install companion separately: pip install -e packages/harness_loop

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
